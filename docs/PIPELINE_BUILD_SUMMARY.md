# MouthBeat Machine

> Build summary generated by pipeline at 2026-05-02 05:10 UTC · 13 tickets · $9.6470 total spend

## Original brief

**Goal:** Goal: Build a phone-first music creation app that lets users record beats, rhythms, beatboxing, vocal percussion, taps, clicks, and other mouth-made sounds, then cleans them up into editable beat patterns on a music timeline.

The app should feel fun, visual, and immediate, but it should also be capable of producing serious beat ideas that can later be exported and used in real music workflows.

Plain English version:

> I want to open the app, make a beat with my mouth, have the app clean it up, map the sounds to drum lanes like kick/snare/hat, snap the timing to a grid, and let me edit/play/export the result.

<details><summary>Full brief (markdown)</summary>

### Goal

Build a phone-first music creation app that lets users record beats, rhythms, beatboxing, vocal percussion, taps, clicks, and other mouth-made sounds, then cleans them up into editable beat patterns on a music timeline.

The app should feel fun, visual, and immediate, but it should also be capable of producing serious beat ideas that can later be exported and used in real music workflows.

Plain English version:

> I want to open the app, make a beat with my mouth, have the app clean it up, map the sounds to drum lanes like kick/snare/hat, snap the timing to a grid, and let me edit/play/export the result.

### Product Positioning

MouthBeat Machine is a playful mobile beat machine / vocal MPC / rhythm visualizer.

It is not meant to be a full DAW in version 1.

It is not meant to be only a toy either.

The core identity should be:

> Fun to record with. Serious enough to make usable beats.

### Target Users

- People who can imagine beats but do not know how to produce them in a DAW
- Musicians who want to capture quick rhythm ideas
- Beatboxers who want to convert performances into structured drum patterns
- Casual creators who want a fun visual music tool
- Producers who want a fast mobile sketchpad
- Content creators who want quick loops for short-form videos

### Core MVP Flow

1. User opens the app.
2. User taps Record.
3. App gives a short countdown.
4. User makes mouth beat sounds into the phone microphone.
5. App shows a live waveform, meter, or reactive visualizer while recording.
6. User stops recording.
7. App analyzes the recording.
8. App detects likely beat hits.
9. App guesses the lane/type for each hit:
   - kick
   - snare
   - hat
   - percussion
   - unknown
10. App snaps detected hits to a musical grid.
11. App creates editable timeline events.
12. User can fix mistakes with touch controls.
13. User can play the cleaned beat using built-in samples.
14. User can change drum kit/sample sounds.
15. User can save the project locally.
16. User can export project data, and later MIDI/WAV.

### Constraints

- Build as a mobile-first app.
- Use React Native.
- Use TypeScript.
- Prefer Expo or Expo Dev Client for the first build.
- Must be usable on phones first, not desktop first.
- MVP should work locally/on-device as much as possible.
- Do not require user accounts for MVP.
- Do not require cloud storage for MVP.
- Do not require backend services for MVP.
- Do not start with complex machine learning.
- Do not build a full DAW in version 1.
- Keep audio logic separate from UI logic.
- Keep project data serializable as JSON.
- Keep the audio engine behind interfaces so a native iOS/Android audio engine can be added later.
- Initial classification can use heuristics instead of trained AI.
- Manual correction must be fast because automatic detection/classification will not be perfect.

### Recommended Tech Stack

#### Mobile App

- React Native
- Expo or Expo Dev Client
- TypeScript
- React Navigation
- Zustand for state management
- React Native Gesture Handler
- Reanimated
- React Native Skia for waveform, timeline, pads, and visualizer rendering

#### Audio

Initial MVP:

- Expo audio/recording APIs for microphone recording and playback
- Local file storage for recordings
- Basic waveform generation
- Basic onset/hit detection
- Sample-based drum playback

Later serious audio engine:

- iOS: AVAudioEngine
- Android: Oboe or another low-latency native audio path
- Offline render path for WAV export
- More accurate scheduling
- Better sample playback and mixing

#### Persistence

MVP:

- Local project save/load
- Project JSON serialization
- Local recording file references

Later:

- Accounts
- Cloud sync
- Shared projects
- Sample pack downloads

### App Screens

#### Home Screen

Purpose:
Get the user into recording quickly.

Should include:

- App name/logo
- Short tagline
- Start Recording button
- Open Recent Project button
- Demo Project button

#### Record Screen

Purpose:
Capture mouth-made beat audio.

Should include:

- Microphone permission state
- Countdown: 3, 2, 1
- Large record/stop button
- Recording timer
- Live input meter
- Reactive waveform or visualizer
- Optional metronome toggle
- BPM selector/input
- Retry option

#### Analyze Screen

Purpose:
Show what the app detected and allow re-analysis.

Should include:

- Recorded waveform
- Detected hit markers
- Sensitivity slider
- Detection summary
- Button: Auto Clean
- Button: Try Again
- Button: Edit Timeline

#### Timeline Screen

Purpose:
Let the user edit the cleaned beat.

Should include:

- Horizontal scroll timeline
- Pinch-to-zoom grid
- Beat/bar markers
- Lanes:
  - kick
  - snare
  - hat
  - percussion
  - unknown
- Draggable events
- Tap event to edit sound/lane/velocity
- Long press event to delete/duplicate/lock
- Transport controls
- Loop toggle
- BPM control
- Quantize strength control
- Swing control
- Humanize control

#### Pads Screen

Purpose:
Make the app fun and playable like a small mobile MPC.

Should include:

- 4 to 16 playable pads
- Each pad triggers a sample
- Pads animate on hit
- User can record pad performance into the timeline later
- User can map mouth-recorded sounds to pads later

#### Visualizer Screen

Purpose:
Make the app feel like a music machine/visualizer.

Should include:

- Full-screen reactive visuals
- Mic-reactive mode
- Playback-reactive mode
- Beat grid pulse
- Theme/preset selection
- Quick record overlay

#### Kit/Sound Screen

Purpose:
Let users choose better sounds.

Should include:

- Built-in drum kits
- Sample preview
- Assign sample to lane
- Lane volume controls
- Mute/solo controls

#### Export Screen

Purpose:
Let users save or move the beat elsewhere.

Should include:

- Save project locally
- Export project JSON
- Export MIDI later
- Export WAV later

### Data Model

```ts
export type LaneType =
  | 'kick'
  | 'snare'
  | 'hat'
  | 'perc'
  | 'bass'
  | 'sample'
  | 'extra'
  | 'unknown';

export interface MouthBeatProject {
  id: string;
  name: string;
  bpm: number;
  timeSignature: [number, number];
  createdAt: string;
  updatedAt: string;
  rawRecording?: RawRecordingRef;
  lanes: TimelineLane[];
  events: TimelineEvent[];
  kit: DrumKitConfig;
  visualizerPresetId?: string;
}

export interface RawRecordingRef {
  id: string;
  durationSeconds: number;
  sampleRate: number;
  localUri?: string;
  waveformPeaks?: number[];
}

export interface TimelineLane {
  id: string;
  type: LaneType;
  label: string;
  volume: number;
  muted: boolean;
  solo: boolean;
  sampleId?: string;
  colorToken?: string;
}

export interface TimelineEvent {
  id: string;
  laneId: string;
  laneType: LaneType;
  startBeat: number;
  originalTimeSeconds: number;
  quantizedTimeSeconds: number;
  durationBeats: number;
  velocity: number;
  confidence: number;
  sourceSliceId?: string;
  selectedSampleId?: string;
  locked?: boolean;
}

export interface DrumKitConfig {
  id: string;
  name: string;
  samples: DrumSample[];
}

export interface DrumSample {
  id: string;
  laneType: LaneType;
  name: string;
  localUri: string;
}
```

### Audio Pipeline

#### Step 1 — Capture Audio

The user records audio through the phone microphone.

MVP requirements:

- Ask for microphone permission.
- Record locally.
- Save recording reference in project state.
- Show recording duration.
- Limit max recording length.

Recommended first max recording length:

- 15 to 30 seconds

#### Step 2 — Generate Waveform

After recording, generate a lightweight waveform representation.

Store waveform as an array of normalized peak values.

Purpose:

- Display waveform quickly
- Show detected hits visually
- Avoid needing to fully decode/render the audio every time

#### Step 3 — Preprocess Audio

Prepare the recording for analysis.

Basic MVP preprocessing:

- Convert to mono if needed
- Normalize volume
- Apply a simple noise gate
- Split into short analysis windows

#### Step 4 — Detect Hits / Onsets

Detect likely beat hit moments.

MVP approach:

- Use amplitude/envelope peak detection
- Look for sudden energy increases
- Ignore peaks too close together
- Store timestamp, energy, and confidence

Each detected hit should include:

```ts
interface DetectedHit {
  id: string;
  timeSeconds: number;
  energy: number;
  confidence: number;
}
```

#### Step 5 — Classify Hits

Classify each hit into a likely lane.

MVP heuristic approach:

- Kick-like: lower frequency/plosive energy, sounds like “boom,” “buh,” “puh”
- Snare-like: noisy mid/high energy, sounds like “kah,” “tah,” “psh”
- Hat-like: short high-frequency energy, sounds like “ts,” “tss,” “ch”
- Perc-like: other short transient sounds
- Unknown: unclear sounds

Important:
Classification will be imperfect. The UI must make it easy to correct.

#### Step 6 — Quantize Timing

Snap events to a music grid.

MVP settings:

- BPM
- Grid size: default 1/16 note
- Quantize strength: 0–100%
- Swing: optional but desirable
- Humanize: later

Store both:

- original timestamp
- quantized timestamp/start beat

#### Step 7 — Create Timeline Events

Convert detected/classified hits into editable timeline events.

Each event should have:

- lane
- startBeat
- originalTimeSeconds
- quantizedTimeSeconds
- velocity
- confidence
- selected sample

#### Step 8 — Playback

Use sample-based playback.

MVP playback:

- Built-in drum samples
- Kick/snare/hat/perc lanes
- Play/stop/loop
- Lane mute/solo
- Lane volume

Important:
Do not depend on React render timing for playback.

#### Step 9 — Export

MVP:

- Project JSON export

Later:

- MIDI export
- WAV export

### State Architecture

Use separate stores.

Recommended stores:

```txt
src/state/projectStore.ts
src/state/audioStore.ts
src/state/playbackStore.ts
src/state/uiStore.ts
```

#### projectStore

Responsible for:

- Current project
- BPM
- Lanes
- Events
- Kit selection
- Save/load project

#### audioStore

Responsible for:

- Mic permission state
- Recording state
- Current recording URI
- Waveform peaks
- Analysis status

#### playbackStore

Responsible for:

- Playing/stopped state
- Playhead position
- Loop state
- Selected loop region

#### uiStore

Responsible for:

- Selected event
- Timeline zoom
- Timeline scroll
- Active tool
- Visualizer preset

### Suggested File Structure

```txt
mouthbeat-machine/
  src/
    app/
      App.tsx
      navigation.tsx
    screens/
      HomeScreen.tsx
      RecordScreen.tsx
      AnalyzeScreen.tsx
      TimelineScreen.tsx
      PadsScreen.tsx
      VisualizerScreen.tsx
      KitScreen.tsx
      ExportScreen.tsx
      ProjectListScreen.tsx
    components/
      transport/
      timeline/
      waveform/
      recorder/
      pads/
      kit/
      visualizer/
      controls/
    state/
      projectStore.ts
      audioStore.ts
      playbackStore.ts
      uiStore.ts
    audio/
      AudioRecorderService.ts
      AudioPlaybackService.ts
      AudioAnalysisService.ts
      AudioExportService.ts
      types.ts
    analysis/
      preprocess.ts
      waveform.ts
      onsetDetection.ts
      featureExtraction.ts
      classifyHit.ts
      quantize.ts
      createTimelineEvents.ts
    persistence/
      projectStorage.ts
      projectSerialization.ts
      migrations.ts
    export/
      exportProjectJson.ts
      exportMidi.ts
      exportWav.ts
    types/
      project.ts
      audio.ts
      timeline.ts
      kit.ts
    assets/
      samples/
        default-kit/
          kick.wav
          snare.wav
          hat.wav
          perc.wav
    theme/
      colors.ts
      spacing.ts
      typography.ts
  package.json
  README.md
```

### UI Style Direction

The visual style should be fun but not childish.

Use a dark/futuristic music-machine style.

Suggested style traits:

- Dark background
- Neon waveform accents
- Large touch-friendly controls
- Animated pads
- Reactive grid pulses
- Rounded cards/buttons
- High-contrast event blocks
- Visual rhythm feedback

Avoid:

- Corporate dashboard feel
- Too many forms
- Tiny DAW controls
- Overly playful children’s-app styling

### Success Criteria

#### MVP Success Criteria

- App runs on a phone simulator or physical phone.
- User can record microphone audio.
- User can see recording feedback.
- User can stop and review recording.
- App can generate a waveform representation.
- App can detect basic hit timestamps from recording.
- App can create timeline events from detected hits.
- App can classify events into kick/snare/hat/perc/unknown with simple heuristics.
- App can quantize events to a BPM grid.
- User can edit events on a mobile timeline.
- User can play the cleaned beat with built-in samples.
- User can save/load project JSON locally.
- App remains usable even when detection is imperfect.
- Manual correction is fast and clear.

#### Serious Beat Output Criteria

- Timing can be cleaned to a grid.
- Events can be manually corrected.
- Samples sound better than the raw mouth recording.
- User can change drum sounds.
- User can export project data.
- Future MIDI/WAV export path is clear.

### Out of Scope

- Authentication
- Cloud accounts
- Cloud project storage
- Social feed
- Marketplace
- Paid subscriptions
- Full DAW arrangement view
- Advanced mixing/mastering
- Plugin/VST support
- Real-time multiplayer
- AI-generated full songs
- Perfect beatboxing transcription
- Humming-to-melody conversion
- Vocal pitch correction
- Native low-latency engine in the very first prototype
- App Store payment systems

### Risks

#### Detection Quality

Mouth sounds vary widely. Automatic classification will be wrong sometimes.

Mitigation:

- Make correction fast.
- Show confidence.
- Support unknown lane.
- Let users re-run detection with sensitivity changes.
- Add guided recording later.

#### Mobile Audio Complexity

React Native audio can become limited for serious timing and rendering.

Mitigation:

- Start with simple recording/playback.
- Keep audio behind interfaces.
- Plan native engine later.

#### Timeline Editing on Small Screens

Precise editing can be hard on phones.

Mitigation:

- Use large event blocks.
- Support pinch zoom.
- Support snap-to-grid.
- Use bottom sheets for event editing.
- Avoid tiny controls.

#### Scope Creep

This can easily become a full DAW.

Mitigation:

- Focus v1 on mouth beat to drum timeline.
- Save melody/humming for later.
- Save cloud/social for later.

### Build Phases

#### Phase 1 — Static Mobile Prototype

Goal:
Build the app shell and core screens with fake data.

Deliverables:

- React Native project setup
- Navigation
- Home screen
- Record screen mock
- Analyze screen mock
- Timeline screen mock
- Pads screen mock
- Visualizer screen mock
- Fake project data

#### Phase 2 — Fake Beat Playback

Goal:
Prove the beat timeline and sample playback concept before real analysis.

Deliverables:

- Default drum kit samples
- Hardcoded timeline events
- Play/stop/loop controls
- Lane volume/mute/solo basics

#### Phase 3 — Real Recording

Goal:
Capture real microphone audio.

Deliverables:

- Mic permission handling
- Start/stop recording
- Recording timer
- Save local recording URI
- Retry recording

#### Phase 4 — Waveform Display

Goal:
Show recorded audio visually.

Deliverables:

- Generate waveform peaks
- Render waveform
- Show playhead/scrub marker
- Show recording duration

#### Phase 5 — Basic Hit Detection

Goal:
Detect likely beat hits.

Deliverables:

- Preprocess audio
- Envelope/peak detection
- Sensitivity slider
- Display detected hit markers
- Store detected hits

#### Phase 6 — Classification and Quantization

Goal:
Turn detected hits into cleaned events.

Deliverables:

- Classify hits into lanes
- Calculate velocity
- Quantize to grid
- Generate timeline events
- Show confidence/unknown states

#### Phase 7 — Timeline Editing

Goal:
Let users fix and shape the beat.

Deliverables:

- Drag event timing
- Move event between lanes
- Delete event
- Add event
- Edit velocity
- Change lane/type
- Snap-to-grid behavior

#### Phase 8 — Save and Export

Goal:
Persist and export work.

Deliverables:

- Save project locally
- Load project locally
- Export project JSON
- Stub MIDI export path
- Stub WAV export path

#### Phase 9 — Polish

Goal:
Make it feel fun and usable.

Deliverables:

- Better animations
- Better visualizer
- Improved styling
- Haptic feedback where appropriate
- Demo project
- Empty/error states
- Onboarding hints

### Initial Task List For Claude Code

#### Task 001 — Initialize App

Create a React Native + Expo + TypeScript app named MouthBeat Machine. Add navigation, basic theme files, and placeholder screens for Home, Record, Analyze, Timeline, Pads, Visualizer, Kit, Export, and Project List.

#### Task 002 — Add Project Data Types

Create TypeScript types for project, lanes, events, raw recordings, drum kits, and samples. Add a fake default project with hardcoded beat events.

#### Task 003 — Add Zustand Stores

Create projectStore, audioStore, playbackStore, and uiStore. Wire the fake project into projectStore.

#### Task 004 — Build Home Screen

Create a mobile-first home screen with app name, tagline, Start Recording button, Demo Project button, and Recent Projects placeholder.

#### Task 005 — Build Timeline Mock

Create a timeline screen using fake project events. Show lanes for kick, snare, hat, percussion, and unknown. Render events as touch-friendly blocks.

#### Task 006 — Build Transport Controls

Add play, stop, loop, BPM display/control, and playhead state. Playback can be fake at first if real audio is not ready.

#### Task 007 — Add Sample Playback Service

Create an AudioPlaybackService interface and simple implementation that can trigger built-in samples for kick, snare, hat, and percussion.

#### Task 008 — Connect Timeline Playback

Use fake project events to trigger sample playback. Keep scheduling logic outside React components.

#### Task 009 — Build Record Screen

Create a recording screen with mic permission state, countdown, record/stop button, timer, and live visual placeholder.

#### Task 010 — Add Real Recording Service

Create AudioRecorderService to request permission, start recording, stop recording, and return local recording metadata.

#### Task 011 — Add Waveform Generation

Create waveform peak generation from recorded audio or a temporary decoded representation. Store peaks in audioStore/project state.

#### Task 012 — Render Waveform

Render waveform peaks on the Analyze screen. Show detected hit markers if present.

#### Task 013 — Add Basic Hit Detection

Create onsetDetection.ts with simple amplitude/envelope peak detection. Add sensitivity controls and store DetectedHit results.

#### Task 014 — Create Hit Classification

Create classifyHit.ts using basic heuristics to assign kick/snare/hat/perc/unknown and confidence.

#### Task 015 — Add Quantization

Create quantize.ts to snap hit times to beat/grid positions based on BPM and grid size. Store original and quantized timing.

#### Task 016 — Generate Timeline Events From Recording

Create createTimelineEvents.ts that converts detected/classified/quantized hits into TimelineEvent objects.

#### Task 017 — Add Manual Timeline Editing

Allow users to drag events, move between lanes, delete events, add events, and change lane/type from a bottom sheet.

#### Task 018 — Build Pads Screen

Create 4 to 16 drum pads that trigger current kit samples. Add pad animations.

#### Task 019 — Build Kit Screen

Create a simple kit/sample selection screen. Allow assigning default sounds per lane.

#### Task 020 — Save/Load Local Projects

Create projectStorage.ts to save and load project JSON locally.

#### Task 021 — Export Project JSON

Create exportProjectJson.ts and connect it to Export screen.

#### Task 022 — Polish Mobile UX

Add haptics, better animations, better visualizer styling, empty states, error states, and demo project onboarding.

### Handoff Template

Use this format at the end of every Claude Code task:

```md
### Handoff — [Task Name]

#### Completed
- List what was completed.

#### Files Changed
- List files changed.

#### How To Test
- Give exact commands or app steps.

#### Known Issues
- List anything incomplete or questionable.

#### Next Suggested Task
- Recommend the next task.
```

### Final Recommendation

Build the first version as a local-first mobile app.

Do not start with accounts, backend, subscriptions, or advanced AI.

The first product win is this:

> The user can make a messy beat with their mouth, tap one button, and get a cleaned-up beat they can edit and play back with real drum sounds.

</details>

## Repository

- **Repo:** https://github.com/pipeline-mcvworks/meatbox
- **Default branch:** `main`

## Run / deploy

No custom recipe set. The post-merge gate uses a language-derived default (Node: `npm install --silent && npm run build --if-present`; Python: `python -m compileall .`; otherwise: skipped). For first-run, follow the repo's README.

**Before running locally**, check the repo's README and `.env.example` (if any) for required environment variables, API keys, and platform-specific setup (e.g. mobile simulators, native dependency rebuilds for Electron, browser permissions).

## Tickets (13)

| # | Phase | Status | Title | Cost | Coder runs | Reviewer runs |
|---|-------|--------|-------|------|------------|---------------|
| `T-001` | 1 | complete | Initialize Expo React Native TypeScript app with navigation and theme ([PR #1](https://github.com/pipeline-mcvworks/meatbox/pull/1)) | $0.1784 | 2 | 1 |
| `T-002` | 1 | complete | Define project/timeline/audio/kit TypeScript types and fake default project ([PR #2](https://github.com/pipeline-mcvworks/meatbox/pull/2)) | $0.0928 | 2 | 1 |
| `T-003` | 1 | complete | Create Zustand stores for project, audio, playback, and UI state ([PR #3](https://github.com/pipeline-mcvworks/meatbox/pull/3)) | $0.2054 | 1 | 1 |
| `T-004` | 1 | complete | Build Home screen and Timeline screen with fake-data rendering ([PR #4](https://github.com/pipeline-mcvworks/meatbox/pull/4)) | $0.0467 | 2 | 1 |
| `T-005` | 2 | complete | Implement AudioPlaybackService with sample playback and transport controls ([PR #5](https://github.com/pipeline-mcvworks/meatbox/pull/5)) | $0.1902 | 1 | 1 |
| `T-006` | 3 | complete | Build Record screen with AudioRecorderService and live feedback ([PR #8](https://github.com/pipeline-mcvworks/meatbox/pull/8)) | $0.9558 | 5 | 2 |
| `T-007` | 4 | complete | Generate and render waveform peaks on Analyze screen ([PR #9](https://github.com/pipeline-mcvworks/meatbox/pull/9)) | $0.5206 | 5 | 3 |
| `T-008` | 5 | complete | Implement onset detection, heuristic classification, and quantization pipeline ([PR #10](https://github.com/pipeline-mcvworks/meatbox/pull/10)) | $0.4399 | 1 | 1 |
| `T-009` | 5 | complete | Convert detected hits into timeline events and wire Auto Clean ([PR #11](https://github.com/pipeline-mcvworks/meatbox/pull/11)) | $1.0849 | 3 | 3 |
| `T-010` | 6 | complete | Add manual timeline editing: drag, lane change, velocity, add/delete ([PR #12](https://github.com/pipeline-mcvworks/meatbox/pull/12)) | $3.1741 | 7 | 1 |
| `T-011` | 6 | complete | Build Pads and Kit screens with sample assignment ([PR #7](https://github.com/pipeline-mcvworks/meatbox/pull/7)) | $0.0530 | 1 | 1 |
| `T-012` | 7 | complete | Implement local project persistence and JSON export ([PR #13](https://github.com/pipeline-mcvworks/meatbox/pull/13)) | $0.4902 | 1 | 1 |
| `T-013` | 8 | complete | Polish: visualizer screen, haptics, animations, demo project, empty states ([PR #14](https://github.com/pipeline-mcvworks/meatbox/pull/14)) | $1.9989 | 5 | 3 |

## Notable-history tickets

Tickets where the architect's plan didn't perfectly fit reality. Worth a careful look on retrospect — these are where the operator spent the most attention.

- `T-001` — Initialize Expo React Native TypeScript app with navigation and theme · 2× coder runs
- `T-002` — Define project/timeline/audio/kit TypeScript types and fake default project · 2× coder runs
- `T-004` — Build Home screen and Timeline screen with fake-data rendering · 2× coder runs
- `T-006` — Build Record screen with AudioRecorderService and live feedback · 5× coder runs · 2× reviewer runs
- `T-007` — Generate and render waveform peaks on Analyze screen · 5× coder runs · 3× reviewer runs
- `T-009` — Convert detected hits into timeline events and wire Auto Clean · 3× coder runs · 3× reviewer runs
- `T-010` — Add manual timeline editing: drag, lane change, velocity, add/delete · 7× coder runs · 1× research note
- `T-013` — Polish: visualizer screen, haptics, animations, demo project, empty states · 5× coder runs · 3× reviewer runs

## Cost

- **Total:** $9.6470
- **By role:**
  - coder: $8.2544
  - reviewer: $1.0990
  - architect: $0.2161
  - researcher: $0.0775
- **Top tickets by cost:**
  - `T-010` ($3.1741) — Add manual timeline editing: drag, lane change, velocity, add/delete
  - `T-013` ($1.9989) — Polish: visualizer screen, haptics, animations, demo project, empty states
  - `T-009` ($1.0849) — Convert detected hits into timeline events and wire Auto Clean
  - `T-006` ($0.9558) — Build Record screen with AudioRecorderService and live feedback
  - `T-007` ($0.5206) — Generate and render waveform peaks on Analyze screen

## Cycle metrics

- **Brief submitted:** 2026-05-02 01:27 UTC
- **First ticket dispatched:** 2026-05-02 01:35 UTC
- **Last ticket landed:** 2026-05-02 04:49 UTC
- **Wall time (brief → last merge):** 3h 22m
- **Active dispatch time (first start → last merge):** 3h 14m
- **Operator interventions:** 2× scope_extension

## Known gaps

Items the reviewer flagged as advisory (non-blocking) or that the coder noted weren't done. The merge landed regardless; these are worth tracking for a follow-up iteration.

**Reviewer advisories:**
- `T-001` (acceptance criteria) @ `src/app/Navigation.tsx`: The AC states 'Navigation can reach all 9 placeholder screens.' Visualizer, Export, and ProjectList are only reachable as stack screens pushed from within the app — there is no UI affordance (button, link, etc.) on any placeholder screen to
- `T-001` (diff smell) @ `app.json`: app.json references ./assets/icon.png, ./assets/splash.png, ./assets/adaptive-icon.png, and ./assets/favicon.png which are not committed. The coder acknowledged this in open_questions. Expo Go development will work, but a production build w
- `T-001` (handoff mismatch) @ `src/app/Navigation.tsx`: The handoff describes a 'stack + bottom tabs' structure and the README navigation diagram shows it correctly, but the handoff summary says 'stack/tab structure' without clarifying that Visualizer, Export, and ProjectList are stack-only scre
- `T-002` (diff smell) @ `src/fixtures/defaultProject.ts`: The JSON round-trip assertion is described only in a comment rather than an actual test file. This is fine for a fixture-only ticket, but a lightweight test in e.g. src/fixtures/__tests__/defaultProject.test.ts would give real CI coverage o
- `T-003` (handoff mismatch) @ `src/state/types.ts`: The handoff mentions 'AnalysiStatus' (typo) as a shared type name, but the diff correctly spells it 'AnalysisStatus' in types.ts. This is a minor documentation inconsistency in the handoff only; the code itself is correct.
- `T-003` (diff smell) @ `src/state/projectStore.ts`: initialState in projectStore.ts uses `new Date().toISOString()` at module load time for createdAt/updatedAt. This means the timestamps are fixed at import time rather than being truly dynamic, which is fine for a default project but worth n
- `T-003` (diff smell) @ `src/state/__tests__/stores.test.ts`: getInitialState() is used in test beforeEach resets. As the coder noted, this requires Zustand v4.4+. If the project pins an older version this will silently fail. Consider adding a package.json version check or a comment asserting the mini
- `T-004` (diff smell) @ `src/components/timeline/TimelineCanvas.tsx`: The header beat-markers ScrollView and each lane's content ScrollView are independent, so horizontal scroll positions are not synchronized. A user scrolling the lane content will not move the beat-marker header in sync, making the bar/beat 
- `T-004` (diff smell) @ `src/screens/TimelineScreen.tsx`: Play and Stop buttons in TimelineScreen have no-op onPress handlers (`() => {}`). The ticket says 'no editing yet; read-only display', so this is acceptable for Phase 1, but the buttons may confuse users into thinking playback is functional
- `T-004` (handoff mismatch) @ `src/screens/HomeScreen.tsx`: The handoff states the coder 'assumed' projectStore exists at src/store/projectStore.ts with loadDefaultProject, currentProject, and event types, but no changes to that file appear in the diff. If the store doesn't already have these fields
- `T-005` (acceptance criteria) @ `src/audio/AudioPlaybackService.ts`: Real WAV sample files (kick.wav, snare.wav, hat.wav, perc.wav) are absent from src/assets/samples/. The require() calls in AudioPlaybackService.ts will cause a Metro bundler error at build time (not just a silent runtime warning) because Me
- `T-005` (diff smell) @ `src/screens/TimelineScreen.tsx`: useProjectStore.getState().updateProjectBpm?.() is called inside a useCallback in a React component. Calling a Zustand store's getState() imperatively is fine, but the optional-chaining means a missing action silently no-ops with no feedbac
- `T-005` (diff smell) @ `src/screens/TimelineScreen.tsx`: The useEffect that syncs BPM from the project has [project?.bpm] as its dependency but references playbackState.isPlaying from the outer closure without including it in the dependency array. If playbackState.isPlaying changes without projec
- `T-005` (diff smell) @ `src/components/transport/TransportBar.tsx`: TransportBar references colors.neonYellow and colors.textSecondary which are not mentioned in the coder's open_questions about the theme shape (only neonGreen and neonPink are listed). If these keys are absent from the theme object, the BPM
- `T-005` (diff smell) @ `src/audio/AudioPlaybackService.ts`: After a loop wrap in _tick(), nextEventIndex is reset to 0 and startTimeMs is set to nowMs, but events that were already scheduled via setTimeout in the previous loop iteration may still fire after the wrap, potentially double-triggering ea
- `T-006` (acceptance criteria) @ `src/screens/RecordScreen.tsx`: The ticket requires 'Recording metadata is persisted in projectStore.rawRecording'. The diff uses a defensive dynamic require() guard and calls projectStore.setRawRecording() only if it exists. If projectStore is absent or has a different A
- `T-006` (diff smell) @ `src/screens/RecordScreen.tsx`: handleStop is referenced inside the setInterval callback in startTimer() via a closure, but handleStop is defined with useCallback after startTimer. This creates a stale-closure risk: the handleStop captured at the time startTimer runs may 
- `T-006` (diff smell) @ `src/audio/AudioRecorderService.ts`: The metering interval in AudioRecorderService is polled manually via setInterval at 100ms in addition to passing a status callback interval of 100ms to Audio.Recording.createAsync(). This results in double-polling the recording status. The 
- `T-007` (diff smell) @ `src/analysis/waveform.ts`: The 100ms playAsync/setTimeout/stopAsync window is very short and likely won't capture samples spanning the full recording, so the resulting peaks may represent only the first fraction of the audio rather than the whole file. Functionally t
- `T-007` (diff smell) @ `src/analysis/waveform.ts`: sound.playAsync() is called without muting/zero-volume, so on devices that do support sample callbacks the user could briefly hear ~100ms of the recording whenever Analyze loads. Consider setVolumeAsync(0) or setIsMutedAsync(true) before pl
- `T-008` (diff smell) @ `src/screens/AnalyzeScreen.tsx`: PanResponder is created inline inside the SensitivitySlider render function on every render. This is harmless but could cause subtle issues; wrapping it in useMemo or useRef is the idiomatic approach. Not a blocker.
- `T-008` (diff smell) @ `src/analysis/featureExtraction.ts`: getBinMagnitude in featureExtraction.ts computes a DFT naively (O(N²) per bin × binCount bins), which will be slow for longer windows. This is a performance concern for future tickets but does not violate any acceptance criterion.
- `T-008` (handoff mismatch) @ `src/screens/AnalyzeScreen.tsx`: The handoff says 'Did not decode full-resolution PCM in AnalyzeScreen; detection runs against the generated waveform peaks'. This is accurately reflected in the diff and is a reasonable limitation, but reviewers of future tickets should not
- `T-009` (diff smell) @ `src/state/projectStore.ts`: Unknown lane still uses sampleId 'sample-clap', so unknown-classified events will trigger the clap sample on playback. Consider a dedicated/no-op sample in a follow-up.
- `T-010` (handoff mismatch) @ `src/components/timeline/EventEditSheet.tsx`: The handoff mentions that EventEditSheet and TimelineScreen detect @gorhom/bottom-sheet via try/require at runtime. This is accurately reflected in the diff, but the open_questions section flags that the package may not be in package.json. 
- `T-010` (diff smell) @ `src/components/timeline/TimelineEventView.tsx`: TimelineEventView defines commitMove, openEditor, and openContextMenu as plain functions inside the component body and passes them to runOnJS inside useMemo callbacks. Because these functions are recreated on every render, the eslint-disabl
- `T-010` (diff smell) @ `src/components/timeline/TimelineLaneRow.tsx`: TimelineLaneRow defines addEventAtX inside the component body and references it inside a useMemo for bgTap, but addEventAtX is not in the dependency array (suppressed with eslint-disable). The function reads pxPerBeat and lane.id from the c
- `T-010` (acceptance criteria) @ `src/screens/TimelineScreen.tsx`: The AC states 'Tap-to-edit bottom sheet changes velocity audibly on next playback.' The velocity change is written to the store via updateEvent, and buildSchedulerEvents now multiplies lane volume by evt.velocity, so the wiring is present. 
- `T-011` (acceptance criteria) @ `src/screens/KitScreen.tsx`: The Kit screen integrates KitList and LaneControls but does not surface SamplePicker anywhere in KitScreen.tsx — there is no UI entry point to open the modal for per-lane sample assignment. The AC requires the user to 'assign a sample to a 
- `T-011` (diff smell) @ `src/components/pads/PadGrid.tsx`: DEFAULT_KIT (4 pads) and BUILT_IN_KITS[0] 'Default' (also 4 pads) are duplicated with identical data. When PadGrid falls back to the PAD_NAMES array because kit.pads.length < 8, it uses generic 'sample-0' … 'sample-15' IDs rather than the n
- _+ 6 more advisories_

**Coder noted as not done:**
- `T-003`: Did not import defaultProject from T-002 as a module (that file may not exist yet); instead inlined a compatible defaultProject shape directly in projectStore.ts to avoid a cross-module dependency that could break compilation
- `T-003`: Did not wire stores to any screen (out of scope)
- `T-004`: Did not add Skia or Reanimated dependencies as the ticket suggested but allowed alternatives; used React Native's built-in ScrollView and Views to avoid adding new dependencies. If Skia/Reanimated is required, a follow-up ticket can refacto
- `T-005`: Real WAV binary sample files could not be created in this text-based handoff — a human must add kick.wav, snare.wav, hat.wav, perc.wav to src/assets/samples/ and update the require() paths in AudioPlaybackService.ts accordingly
- `T-005`: Unit tests for the scheduler were not written — the ticket did not specify a test framework and none exists in the current project
- `T-005`: Precise sub-millisecond audio scheduling (Web Audio API style look-ahead) was not implemented — setInterval at ~10ms granularity is used, which is sufficient for mobile drum machines but not sample-accurate
- `T-006`: Actual metronome audio click track playback — the toggle and BPM input are wired up but playing a click track requires additional audio scheduling work; the UI is present and BPM is stored but clicks are not played (flagged in open_question
- `T-006`: Navigation to AnalyzeScreen — the navigate call is present but AnalyzeScreen is out of scope so we cannot verify the route name matches; used 'Analyze' as the route name
- `T-006`: projectStore integration — projectStore is referenced but its type/shape is unknown from the provided files; we import it defensively and call setRawRecording if available
- `T-007`: Did not add a RawRecordingRef model because no RawRecordingRef file or type is in scope; peaks continue to be stored in the scoped audio store as already wired by the screen.
- `T-007`: Did not implement timeline editing because the ticket specifies Edit Timeline is a no-op for now.
- `T-008`: Did not persist detected or quantized hits because persistence files are explicitly out of scope.
- `T-008`: Did not wire the Edit Timeline action because TimelineScreen is explicitly out of scope.
- `T-008`: Did not decode full-resolution PCM in AnalyzeScreen; detection runs against the generated waveform peaks available within scoped files.
- `T-009`: Did not change the advisory unknown lane sampleId because it was not required by the blocker and changing sample behavior may need a dedicated no-op sample outside this ticket's provided context.
- `T-010`: Did not modify ControlsButton.tsx — the existing component was suitable as-is and not needed for this ticket's flow (the new TransportControls uses sliders, not buttons).
- `T-010`: Did not implement pinch-to-zoom on pxPerBeat — the plan mentioned it but the acceptance criteria don't require it, and adding it would expand scope. uiStore.pxPerBeat is settable so it can be added later.
- `T-010`: Did not wire to the existing audioPlaybackService scheduler refresh on every edit — the original screen rebuilt scheduler events on play, and we preserved that behavior. Live re-scheduling during playback is a separate concern.
- `T-010`: @gorhom/bottom-sheet may not yet be installed; if so, the package needs adding via package.json (out of scope per the ticket's files_in_scope). Flagged in open_questions.
- `T-012`: Did not implement actual MIDI or WAV export — stubbed per ticket
- `T-012`: Did not write automated tests; project has no existing test infrastructure visible in scope
- `T-012`: Did not modify navigation; ProjectList already wired by prior tickets is assumed (only screen contents were in scope)
- `T-013`: VisualizerCanvas SVG dependency not added to package.json — the advisory notes this but package.json changes are in scope; however the fallback path already works without it and adding an undeclared dep could break CI more than help. Left a

---

_Generated by pipeline · 2026-05-02 05:10 UTC · regenerate from the project page in the dashboard or via `POST /projects/051f2656-fc6b-4ed0-abf3-5d52b17360dc/regenerate-summary`._
